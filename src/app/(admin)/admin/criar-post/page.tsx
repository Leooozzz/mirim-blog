import { CreatePostForm } from "@/components/admin/posts/createPost";

export default function Page() {
  return (
    <main className="min-h-screen flex justify-center items-center">
      <CreatePostForm />
    </main>
  );
}
