import { LoginForm } from "@/components/auth/loginForm";

export const Page = () => {
  return (
    <main className="max-w-md mx-auto ">
      <LoginForm />
    </main>
  );
};

export default Page;
